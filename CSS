/* Add rounded styling to the search box */
.search-container {
    position: relative;
    width: 100%;
    max-width: 400px; /* Adjust the max width as needed */
    margin: 0 auto;
    padding: 15px;
    border-radius: 20px; /* Control the roundness */
    background-color: #f0f0f0; /* Background color */
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2); /* Add a subtle shadow */
}

/* Style the search input field */
#search-box {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 20px; /* Match the container's roundness */
    outline: none;
    font-size: 16px;
}

/* Style the search results container */
#search-results {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background-color: #fff;
    border-radius: 0 0 20px 20px; /* Rounded bottom corners */
    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2);
    display: none;
}

/* Style individual search result items if needed */
/* #search-results .result {
    padding: 10px;
    border-bottom: 1px solid #ccc;
} */
